import 'package:flutter/material.dart';

class DummpyPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child :Text("wait until user")),
    );
  }
}